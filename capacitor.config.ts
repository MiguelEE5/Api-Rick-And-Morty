import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Api Rick And Morty Project',
  webDir: 'www'
};

export default config;
